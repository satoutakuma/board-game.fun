<%= form_with model: [@game, GameComment.new], url: create_reply_game_path(@game, parent_comment), local: true do |f| %>
  <%= f.text_area :comment, rows: '5', placeholder: "返信をここに" %>
  <%= f.submit "返信" %>
<% end %>